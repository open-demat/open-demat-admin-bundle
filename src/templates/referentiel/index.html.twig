{% extends 'base.html.twig' %}

{% block title %}Administration des référentiels{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  <style>
    .admin-header{display:flex;flex-wrap:wrap;align-items:baseline;justify-content:space-between;gap:.75rem;margin-bottom:1.25rem}
    .admin-meta{text-align:right;font-size:.875rem;color:var(--muted,#6c757d)}
    .toolbar{display:flex;gap:.75rem;flex-wrap:wrap;align-items:end;justify-content:space-between;margin-bottom:1rem}
    .toolbar .form-control{max-width:320px}
    .mono{font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Courier New",monospace}
    .badge-lite{font-size:.75rem;border-radius:999px;border:1px solid rgba(0,0,0,.08);background-color:rgba(0,0,0,.03);color:#333}
    @media (max-width:575.98px){.admin-meta{text-align:left}.toolbar .form-control{max-width:100%}}
  </style>
{% endblock %}

{% block body %}
<div class="container mt-4">

  <div class="admin-header">
    <div>
      <h1 class="mb-1">Administration des référentiels</h1>
      <p class="text-muted mb-0">
        Détection automatique (Doctrine) des entités héritant de <span class="mono">AbstractReferentiel</span>.
      </p>
    </div>
    <div class="admin-meta">
      <div>Total : <strong>{{ refs|length }}</strong></div>
    </div>
  </div>

  <div class="toolbar">
    <form method="get" class="d-flex flex-wrap gap-2 align-items-end">
      <div>
        <label class="form-label small text-muted mb-1">Recherche</label>
        <input type="search" name="q" value="{{ q }}" class="form-control form-control-sm"
               placeholder="schema.table, entity, …">
      </div>

      <button class="btn btn-sm btn-outline-secondary" type="submit">Rechercher</button>

      <a href="{{ path('open_demat_admin_dashboard') }}" class="btn btn-sm btn-outline-secondary ms-2">
        ← Retour au dashboard
      </a>
    </form>
  </div>

  {# Flash #}
  {% for label, messages in app.flashes %}
    {% for message in messages %}
      <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
        {{ message }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
      </div>
    {% endfor %}
  {% endfor %}

  <div class="table-responsive shadow-sm bg-white rounded-3">
    <table class="table table-hover table-sm mb-0 align-middle">
      <thead>
        <tr>
          <th>Référentiel</th>
          <th>Entity</th>
          <th>Champs</th>
          <th class="text-end" style="width:180px;">Actions</th>
        </tr>
      </thead>
      <tbody>
      {% for def in refs %}
        {% set full = (def.schema ? def.schema ~ '.' : '') ~ def.table %}
        <tr>
          <td>
            <div class="fw-semibold">
              <span class="badge-lite px-2 py-1">{{ full }}</span>
            </div>
            <div class="text-muted small">
              <span class="mono">{{ def.shortName }}</span>
            </div>
          </td>

          <td class="small">
            <div class="mono">{{ def.entityClass }}</div>
          </td>

          <td class="small text-muted">
            {% for f in def.fields|slice(0, 8) %}
              <span class="badge-lite px-2 py-1 me-1">{{ f }}</span>
            {% endfor %}
            {% if def.fields|length > 8 %}
              <span class="text-muted">+{{ def.fields|length - 8 }}</span>
            {% endif %}
          </td>

          <td class="text-end">
            <a class="btn btn-sm btn-outline-primary"
               href="{{ path('open_demat_admin_referentiels_list', { key: full|url_encode }) }}">
              Ouvrir
            </a>
          </td>
        </tr>
      {% else %}
        <tr>
          <td colspan="4" class="text-center text-muted py-4">
            Aucun référentiel détecté.
          </td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  </div>

</div>
{% endblock %}
